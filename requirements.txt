# Lightweight backend API dependencies only
# Heavy ML libs (diffusers, transformers, etc.) run on Colab, not here
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6
requests>=2.31.0
