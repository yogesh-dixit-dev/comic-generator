{
  "name": "comic-generator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix infrastructure/web",
    "build": "npm run build --prefix infrastructure/web",
    "dev": "npm run dev --prefix infrastructure/web"
  },
  "engines": {
    "node": ">=18"
  }
}
